/*
 * kip.hpp - STUB header
 *
 * Retained for compile compatibility with any translation units that reference
 * the kip namespace.  All KIP write/validate logic has been removed.
 */

#pragma once
#include <stdbool.h>

namespace kip {
    extern bool kipAvailable;
    void GetKipData();
} // namespace kip
